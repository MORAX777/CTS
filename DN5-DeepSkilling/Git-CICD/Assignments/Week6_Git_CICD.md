# Week 6 - Git and CI/CD Hands-On

**Candidate Name:** Aryan Kumar Raj
**Roll Number:** 231fa18066

## Assignment Summary
This document summarizes the mandatory hands-on completion for the Git and CI/CD modules as part of Week 6 in the Cognizant Digital Nurture 5.0 Deep Skilling track.

### 1. Git Version Control
- Successfully initialized and maintained the Git repository for the Deep Skilling track.
- Implemented `.gitignore` files to ignore `node_modules`, `dist/`, and other build artifacts.
- Synced codebase with remote repository effectively using `git add`, `git commit`, `git push`, and `git pull`.
- Resolved conflicts, performed branch operations, and maintained clean version history.

### 2. Continuous Integration & Continuous Deployment (CI/CD)
- Created a GitHub Actions workflow pipeline to automate the build process of the Angular Application (`student-course-portal`).
- Configured the workflow (`.github/workflows/angular-ci.yml`) to trigger on `push` and `pull_request` to the main/master branch.
- Automated `npm ci` and `npm run build` steps to verify the integrity of the application code upon every commit.

### 3. Application Updates (Angular)
- The Student Course Portal application continues to be developed per the Angular Hands-on requirements. 
- Integrated CI pipeline ensures robust Angular SPA delivery.

*Note: All code changes have been pushed to the remote repository successfully.*
